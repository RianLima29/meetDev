import styled from "styled-components";
import { COLOR_ACCENT } from "../../config";
import { SiTinder } from "react-icons/si";

export const Logo = styled(SiTinder)`
  color: ${COLOR_ACCENT};
  height: 100%;
  font-size: 45px;
`;
